<%= simple_form_for(@email_project) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :email %>
    <%= f.input :cat %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
